version: '3.8'

services:
  postgres:
    image: postgres:latest
    container_name: database-container
    environment:
      POSTGRES_DB: db
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: pass
    ports:
      - "5432:5432"